<h1>Movies:</h1>

<% @movies.each do |movie| %>
    <div class="row">
        <div class="col text-center">
            <%= link_to movie.title, movie_path(movie), class: "btn btn-primary w-50" %>
        </div>
    </div>
<% end %>

<%= link_to "Back", root_path, class: "btn btn-danger"%>